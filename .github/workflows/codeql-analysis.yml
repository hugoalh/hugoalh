name: "CodeQL - Analysis (Reusable Workflow)"
on:
  workflow_call:
jobs:
  main:
    name: "Main"
    runs-on: "ubuntu-latest"
    if: "${{!(github.event_name == 'pull_request' && startsWith(github.event.pull_request.head.label, format('{0}:', github.repository_owner)))}}"
    steps:
      - name: "Checkout Repository"
        uses: "actions/checkout@v3.5.3"
        with:
          fetch-depth: 0
      - name: "Initialize CodeQL"
        uses: "github/codeql-action/init@v2.21.2"
        with:
          queries: "+security-and-quality"
      # Automatically build attempts to build any compiled languages (C/C++, C#, Go, or Java).
      - name: "Build Repository"
        uses: "github/codeql-action/autobuild@v2.21.2"
        continue-on-error: true
      - name: "Analyze Repository"
        uses: "github/codeql-action/analyze@v2.21.2"
